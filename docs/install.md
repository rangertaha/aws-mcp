# Install

```sh
go install github.com/rangertaha/aws-mcp/cmd/aws@latest
```

This produces an `aws` binary. Or build from source:

```sh
git clone https://github.com/rangertaha/aws-mcp
cd aws-mcp
make build        # produces ./bin/aws
```
